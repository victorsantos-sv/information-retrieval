

def calculate_norm(terms):
    # terms[0].document.norm = 5.068
    #
    # _previous_document = terms[0].document.document_name
    #
    # _norm = None
    # for document_term in terms:
    #     if _previous_document != document_term.document.document_name:
    #
    #
    #     _previous_document = document_term.document.document_name
    print(terms[0])
    # for document_term in terms:
        # [document.document_name for document in terms].index(document_term.document.document_name)
